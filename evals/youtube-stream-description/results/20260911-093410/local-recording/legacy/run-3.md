Here's the suggested title (it isn't part of the description): **Designing the Meta Query Modal for Advanced Query Loop - Live Stream**

```
Deep in the weeds on the new meta query modal for Advanced Query Loop, and chat had opinions. Good ones! We figured out the dynamic placeholders list is eating way too much real estate, so it's getting tucked behind an info button (and losing the search, because there just aren't that many of them). Grouped and nested queries mean this modal needs every pixel it can get.

I also walked through the new meta key and meta value pickers. Existing keys and values now show up automatically, but you can still type your own, because not everyone's meta is registered with the REST API. Then chat pointed me at a searchable chip select with a "createable" option, and I spent a good while digging through props trying to make it do single selection. I'm sure someone will send me the link that shows exactly how to do it two seconds after I stop looking.

00:00 Is the modal taking up too much space? Rethinking the placeholders list
02:00 Auto-populated meta keys & values (and a lot of code Claude clearly wrote)
04:00 Why free-form input matters for meta not registered with the REST API
06:00 Can a searchable chip select do single selection? Digging into createable

Connect:

https://www.twitch.tv/ryanwelchercodes
https://twitter.com/ryanwelcher
https://www.linkedin.com/in/ryanwelcher/
https://bsky.app/profile/ryanwelcher.com

Projects:

👉 Advanced Query Loop plugin: https://wordpress.org/plugins/advanced-query-loop/

#wordpress #wordpresstutorial #coding #customblocks #javascript #php #gutenberg #livestream
```

- **Short clip:** the recording is about 8 minutes, so I used four chapters, one per 2-minute section of the transcript. That meets YouTube's rules: the first chapter starts at 00:00, there are at least three, and each is at least 10 seconds long.
- **Names I left out:** the transcript doesn't clearly name the component chat suggested, so I called it a "searchable chip select." I also left out the chat username, since "bad dude" was probably a mis-transcription.
- **Links and install:** the description has no links beyond the standard template ones. Nothing was installed, because `ffmpeg` and `mlx_whisper` were already there.