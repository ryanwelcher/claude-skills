# Section 1 — TODO Section Title

**Type:** `coding` <!-- coding | tour | demo | hackathon -->
**Target time:** TODO min
**Capability added:** TODO — single sentence naming the one thing the artifact will newly be able to do after this section.

> TODO: 1–2 sentence concept intro. Why does this capability matter for the north-star artifact?

*(Optional: link to relevant docs — e.g., "[useSelect docs](https://developer.wordpress.org/block-editor/reference-guides/packages/packages-data/#useselect)".)*

---

## Starting state

This section starts from the empty scaffold. *(For later sections: replace with "`code-reference/section-{N-1}/`".)*

---

## Steps

<!-- Steps follow Simplified Technical English: one imperative action per step, 20 words max,
     CAUTION/NOTE before the step it applies to, full UI paths. See ste-pass/references/ste-card.md. -->

1. TODO first step — action verb, concrete instruction.

   **`src/path/to/new-file.js`** *(new file)*
   ```js
   // TODO: full file contents — new files get the cumulative state so attendees
   // can create the file and paste this in verbatim.
   ```

2. TODO next step — edit an existing file.

   In `src/path/to/existing-file.js`, add inside `someFunction()`:
   ```js
   // TODO: just the inserted lines — edits get an unambiguous anchor (function
   // name, existing line to insert after, or explicit line range to replace).
   ```

3. TODO next step — replacement.

   In `render.php`, replace the placeholder `return '';` with:
   ```php
   return wp_kses_post( $content );
   ```

4. TODO final step — "Make sure that…" check. What should the attendee see in the editor / on the frontend / in the console?

---

## End state

The artifact now TODO (describe the demoable capability you just added). This state is captured in `code-reference/section-1/` and is the **starting point for the next section** — no hidden setup in between.

✅ 🎉

---

# Ready to move on?
[Section 2: TODO](./section-2.md)

# Missing something from this section?
Copy `code-reference/section-1/` into your working directory to sync up before moving on.
